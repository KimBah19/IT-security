#include <stdio.h>
#include <math.h>

int main()
{
    // Math functions = Funktioner som används för att utföra matematiska beräkningar
    // sqrt = Kvadratroten
    // pow = Potens
    // round = Avrundar till närmaste heltal
    // ceil = Avrundar uppåt
    // floor = Avrundar nedåt
    // fmax = Returnerar det största värdet
    // fmin = Returnerar det minsta värdet
    // fabs = Returnerar absolutbeloppet
    //sin, cos, tan, asin, acos, atan = trigonometriska funktioner

    //Exempelvis

    double A = sqrt(9);
    double B = pow(2, 3);
    int c = round(3.4);
    int d = ceil(3.1);
    int e = floor(3.99);
    double f = fabs(-3.14);
    double g = log(3.14);
    double h = sin(45);
    double i = cos(45);
    double j = tan(45);

    return 0;
}