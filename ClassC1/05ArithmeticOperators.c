#include <stdio.h>

int main()
{
    // Arithmetic operators = Operander + Operator
    // + = Addition
    // - = Subtraktion
    // * = Multiplikation
    // / = Division
    // % = Modulus (resten vid division)

    int x = 10;
    int y = 3;

    int z = x + y; //Addition

    printf("%d", z);
    return 0;
}